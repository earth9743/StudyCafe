import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Firebase 초기화 필요로 인해 기본 스모크 테스트는 추후 구현
    expect(true, isTrue);
  });
}
